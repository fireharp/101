package flippinguniquewords

// flipUnique reads words from next (empty string = done) and returns reversed unique words.
func flipUnique(next func() string) []string {
	// TODO: pipeline with deduplication and reverse stage.
	_ = next
	return nil
}
