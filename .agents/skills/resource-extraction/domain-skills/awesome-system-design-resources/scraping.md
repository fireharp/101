# Awesome System Design Resources — Extraction

Resource: `ashishps1/awesome-system-design-resources`

## Access Path

- Use GitHub API for tree listing.
- Use `raw.githubusercontent.com` for Markdown content.
- Default branch is `main`.

## Include

- `README.md`

## Notes

- Treat this repo as a curated index in v1.
- Extract sections and links from README, but do not deep-crawl external URLs
  until a separate resource skill exists for each external domain.
- Browser extraction is not needed for the GitHub README itself.
